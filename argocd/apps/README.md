# argocd/apps

ArgoCD Application manifests pointing at `helm/demo-app` and
`helm/postgres-values`. Populated in **Stage 4 (ArgoCD)**.

Should also cover the rollback story (see "Open / deferred" in the top-level
README) — not just drift detection.
